name: Helm chart release

on:
  workflow_dispatch:
    inputs:
      force:
        description: "Overwrite the chart version in GHCR even if it already exists"
        type: boolean
        default: false

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
        with: { version: v3.18.6 }
      - name: Lint
        run: helm lint charts/atmosphere
      - name: Render (sanity)
        run: |
          helm template atmosphere charts/atmosphere \
            -f charts/atmosphere/ci/external-only-values.yaml \
            --set externalDatabase.existingSecret=s \
            --set externalRedis.existingSecret=s \
            --set auth.existingSecret=s > /dev/null

  publish:
    needs: lint-test
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
        with: { version: v3.18.6 }
      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Package
        run: helm package charts/atmosphere -d .cr-release-packages
      - name: Push to OCI (skips if the version exists, unless force is set)
        run: |
          VERSION=$(helm show chart charts/atmosphere | awk '/^version:/{print $2}')
          if [ "${{ inputs.force }}" != "true" ] && helm pull oci://ghcr.io/atmosphere/charts/atmosphere --version "$VERSION" >/dev/null 2>&1; then
            echo "Version $VERSION already published; skipping (re-run with force=true to overwrite)."
          else
            helm push ".cr-release-packages/atmosphere-$VERSION.tgz" oci://ghcr.io/atmosphere/charts
          fi
