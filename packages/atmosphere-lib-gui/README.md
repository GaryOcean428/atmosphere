# atmosphere-lib-gui

This package is the build version of ``atmosphere-gui`` which will be used in ``packages/atmosphere``. The built files should be located in ``packages/atmosphere-lib-gui/lib/dist/``. 

To build the frontend and copy the built files to dist directory, run ``npm run build:copy`` under ``packages/atmosphere-gui/``.