{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "atmosphere.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "atmosphere.labels" . | nindent 4 }}
  {{- with include "atmosphere.annotations" (dict "context" . "extraAnnotations" .Values.serviceAccount.annotations) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
{{- end }}
