# Atmosphere Quickstart: local evaluation on Mac, Windows, or Linux.
#
#   docker compose up -d      then open http://localhost:8080
#
# This stack is for trying Atmosphere locally. For a production install with HTTPS,
# automatic SSL, and hardening, use the installer instead:
#
#   bash <(curl -sSL https://install.atmosphere.dev/atmosphere.sh)

services:

  atmosphere:
    image: atmosphere/atmosphere:latest
    environment:
      ATMOSPHERE_DB: 'pg://db:5432?u=atmosphere&p=atmosphere&d=atmosphere'
      ATMOSPHERE_REDIS_URL: 'redis://redis:6379'
      ATMOSPHERE_SITE_URL: 'http://localhost:8080'
      ATMOSPHERE_DISABLE_MUX: 'true'
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - atmosphere_data:/usr/app/data
    networks:
      - atmosphere-network
    ports:
      - '8080:8080'
    healthcheck:
      test: ['CMD-SHELL', 'wget -q --tries=1 --spider http://localhost:8080/api/v1/health || exit 1']
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s

  worker:
    image: atmosphere/atmosphere:latest
    environment:
      ATMOSPHERE_DB: 'pg://db:5432?u=atmosphere&p=atmosphere&d=atmosphere'
      ATMOSPHERE_REDIS_URL: 'redis://redis:6379'
      ATMOSPHERE_SITE_URL: 'http://localhost:8080'
      ATMOSPHERE_WORKER_CONTAINER: 'true'
    depends_on:
      atmosphere:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - atmosphere_data:/usr/app/data
    networks:
      - atmosphere-network

  db:
    image: postgres:17.10
    environment:
      POSTGRES_USER: atmosphere
      POSTGRES_PASSWORD: atmosphere
      POSTGRES_DB: atmosphere
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U atmosphere -d atmosphere']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - atmosphere-network

  redis:
    image: redis:7
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - atmosphere-network

networks:
  atmosphere-network:
    driver: bridge

volumes:
  atmosphere_data:
  postgres_data:
  redis_data:
