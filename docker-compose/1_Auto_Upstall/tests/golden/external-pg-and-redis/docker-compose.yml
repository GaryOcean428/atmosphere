services:

  atmosphere:
    image: atmosphere/atmosphere:latest
    env_file: docker.env
    deploy:
      mode: replicated
      replicas: 1
    restart: unless-stopped
    volumes:
      - atmosphere_data:/usr/app/data
      - ./atmosphere/db.json:/usr/app/data/db.json
    networks:
      - atmosphere-network
    healthcheck:
      test: ['CMD-SHELL', 'wget -q --tries=1 --spider http://localhost:8080/api/v1/health || exit 1']
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    ports:
      - '8080:8080'

  worker:
    image: atmosphere/atmosphere:latest
    env_file: docker.env
    environment:
      ATMOSPHERE_WORKER_CONTAINER: 'true'
    depends_on:
      atmosphere:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - atmosphere_data:/usr/app/data
      - ./atmosphere/db.json:/usr/app/data/db.json
    networks:
      - atmosphere-network

networks:
  atmosphere-network:
    driver: bridge

volumes:
  atmosphere_data:
